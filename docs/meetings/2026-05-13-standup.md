# BlueBird Daily Standup — 2026-05-13

**일시**: 2026-05-13 09:00 KST
**참여자**: 임원 4명 + 호출 산하 3명 (총 7명)
**모드**: Orchestrator (Sonnet 4.6) — Case 2

---

## 0. 부분 조용 (case 2) — git 변화: notifications/cron

**24h git 통계**: 1 commit

| hash | 요약 | 변경 경로 |
|---|---|---|
| `964d50d` | fix(push): cron GET/POST export 누락 — 알림 미발송 root cause | `app/api/cron/checkin-reminder/route.ts`, `tests/notifications/cron.test.ts` |

**원인**: Vercel Cron은 GET 요청 발송 → 기존 POST-only export → 48h 0 cron 도달. 수동 `curl -X POST` sanity check에선 200 OK였으나 실제 cron은 0회 진입.

**수정**: `handleCron()` 공통 로직 추출 + GET·POST 양쪽 export. 테스트 157/157 (cron 5→8, +3 GET guard).

**carry-over 요약**: Open 5건 — CEO 2건 (VAPID 설정, IM.1 모집), senior-fullstack-engineer 1건 (privacy push 항목), senior-qa-engineer 1건 (iOS E2E), all-hands 1건 (23 actions seed).

**agenda**: 없음.

---

## 1. 임원 라운드

### 1.1 CPO

PMF 게이트 진척 기준: 자발 언급 ≥30% / 30일 잔존율 ≥15% / 결제 의향 ≥20% — 3종 동시.

**현 위치**: 베타 0명, IM.1 모집 미시작. 세 게이트 모두 측정 미시작.

이번 `964d50d` fix는 push 알림 → 체크인 참여 → 30일 잔존율 데이터 품질 chain에 영향. 그러나 CEO carry-over VAPID 설정·Vercel 크론 활성화(`due-2026-05-17`)가 완료되지 않으면 코드 fix만으로 프로덕션 발송 불가. 알림 인프라가 실제 작동해야 30일 잔존율 측정이 의미 있다.

**우선순위 1 — Falsifiability**: IM.1 모집 시작이 계속 지연되면 60일 게이트 측정 창이 좁아진다. CEO 2건 carry-over(`due-2026-05-17`)를 이번 주 내 완료 요청.

**우선순위 2 — 비목표 정합**: 이번 변경(push 알림 체크인 리마인더)이 본질 위협 #4 "(정기 자기 라벨링 체크인 추가)"에 해당하지 않는지 점검 — 체크인은 기존 Tier 0 검증된 흐름이며 신규 추가 아님. 정합.

no new PMF signal. 다음 측정 가능 시점: IM.1 모집 후 30일.

### 1.2 CSO

차별화 3축 정합성 점검 (이번 변경: notifications/cron infra):

| 축 | 이번 변경 영향 | 평가 |
|---|---|---|
| 톤 | 없음 (인프라 레이어) | stable |
| 자기상 | 없음 | stable |
| 개입 방식 | 자동화 인프라 유지·강화 | 차별화 보호 |
| 시간 모델 | 없음 | stable |
| 가격 | 없음 | stable |
| 메타포 | 없음 | stable |

**차별화 3축**: no shift detected. 이번 fix는 자동화 개입 방식(디스턴싱의 인간 코치와 반대) 인프라를 정상화 — 오히려 차별화 보호.

**경쟁 동향**:
- 디스턴싱 분석가 톤 진입 시그널: 감지 없음.
- Wysa·Woebot 한국어 본격 진입 시그널: 감지 없음.
- 글로벌 챗봇 Layer: 시간 압박 지속 중이나 이번 주 신규 시그널 없음.

**락인 강화**: push 알림 체크인 루프는 사용자의 패턴 누적 자기 지도(락인 메커니즘 #3)에 기여. 인프라 fix가 락인 chain 복구에 해당.

no strategic alert.

### 1.3 CTO

**hot path 안정성**: `964d50d` — production 사고 (48h cron 0 도달) root cause 확인·수정 완료. Vercel Cron GET + Next.js export 패턴 정합. 이 클래스의 메서드 오류는 `npm run dev`의 Turbopack vs 프로덕션 webpack 차이처럼 런타임 환경 의존 — 로컬 sanity check로 감지 어려운 blind spot.

**회귀 보호**: 테스트 157/157 통과. cron 테스트 케이스 5→8 (+3 GET guard). `npm run lint` clean, `lint:copy` 0. 회귀 게이트 3종 모두 통과.

**migration**: 없음.

**안전 가드 (P0)**: Crisis Detection·RLS·프롬프트 인젝션 방어 — 이번 변경 영향 없음. stable, no incidents.

**기술 우려 1**: 로그 라인(`method=GET|POST sent=N total=N failed=N`)이 추가됐으나 Vercel 로그에 PII(userId 등) 평문 포함 여부 senior-qa-engineer 검증 필요.

**기술 우려 2**: CEO carry-over VAPID 설정 미완료 시 이 fix는 프로덕션에서 여전히 효과 없음. 코드 수준 fix와 환경 설정 fix가 *둘 다* 필요한 two-part repair. CTO 차원에서 CEO 액션 블로커 명시.

**Tier 정합성**: push 알림 체크인 reminder는 Tier 0 검증된 흐름. fix가 Tier 0 복구에 해당 — Tier 정합.

### 1.4 CMO

Stage 0 진척:

**IM.1 모집**: CEO carry-over (50명 1차 초대, `due-2026-05-17`) 미시작. Stage 0 핵심 deliverable 블로커 지속.

**DM 템플릿·랜딩 v0·SMTP stagger**: no movement (모집 인프라 준비 단계 유지).

**자산 stockpile**: no movement.

이번 push 알림 fix 관련 CMO 차원 평가:
- push 알림 체크인 copy(`21시 푸시`)가 외부 채널에 노출되지 않음 — Stage 0 허용 범위 내.
- 본질 위협 #1(카피 정서화)·#3("스트레스 관리" 입구화)·#5(가격 디스턴싱 수렴)·#6("외롭지 않게" 기획 동기): 이번 변경에서 감지 없음.

**Stage 0 미션**: IM.1 모집 인프라 준비 계속 — push 알림 인프라 복구가 완료되면 베타 사용자 체크인 참여 데이터 품질 향상 → IM.1 인터뷰 대상자 행동 지표 측정 기반 마련. CEO 액션 2건 이번 주 완료가 Stage 0→1 진입 전제조건.

---

## 2. 산하 라운드 (case 2 — 트리거)

### 2.1 senior-fullstack-engineer

**영역**: `app/api/cron/checkin-reminder/route.ts`

**24h 변화 리뷰**: `964d50d` — 구조 설명.

- `handleCron(request: Request): Promise<Response>` 공통 로직 추출
- `export async function GET(request: Request)` — Vercel Cron 트리거
- `export async function POST(request: Request)` — 수동 curl 검증용
- 로그: `method=GET|POST sent=N total=N failed=N`

**결제 가설 평가**: push 알림 → 체크인 참여 → Δpain 시계열 데이터 → "내가 변하고 있다는 증거" 시각화 chain. 이 chain에서 알림 누락은 가설 A 측정 체인의 upstream 단절. 이번 fix는 결제 가설 A chain 복구.

**회귀**: `npm run lint` clean, `vitest 157/157`, `lint:copy 0` — 3종 게이트 모두 통과.

**carry-over 진척**: `app/privacy/page.tsx` push subscription 항목 추가 (P3, `due-2026-05-24`) — 미완료. 아래 risk-manager 우려 참조: VAPID 활성화 전 선행 권장.

**우려 1**: 로그 라인에 userId 포함 여부 확인 필요. 현재 `sent=N total=N failed=N` 형식은 집계치만 — PII 평문 없음 확인됨.

**우려 2**: CEO VAPID + Vercel 크론 활성화 없으면 이 fix는 dead code. two-part repair의 part 2 (환경 설정)가 블로커.

### 2.2 risk-manager

**영역**: notifications 인프라 변경 법무 점검

**법규 5축 점검** (`964d50d`):

| 축 | 점검 결과 |
|---|---|
| PIPA | 신규 개인정보 수집 없음. 기존 push subscription endpoint·VAPID 공개키만 사용 — 수집 항목 변화 없음 |
| 의료기기법 | 알림 copy 내용 미확인 — 체크인 리마인더는 기존 Tier 0 흐름. "치료 효과" 암시 없음으로 추정 |
| 표시광고법 | push 알림은 사용자가 직접 구독한 채널 — 광고성 정보 전송 규제 적용 여부 점검 필요 |
| 약관 | 변화 없음 |
| 차별화 충돌 | 없음 |

**P1 우려**: `app/privacy/page.tsx`에 push subscription 데이터 처리 항목 미추가 상태. VAPID 설정 완료 → 실제 push 발송 재개 시 PIPA §24(제3자 제공·위탁 고지) 맥락에서 Vercel/Web Push 서비스 국외이전 고지 의무 충족 여부 불확실. **VAPID 활성화 전 privacy 페이지 업데이트 선행 권장** (기존 P3 → P1 격상 의견 — CTO 결정 요청).

**현재 임시 가드**: 이번 fix 자체는 기존 인프라 정상화이므로 신규 노출 없음. privacy 페이지 gap은 push가 실제로 사용자에게 도달하기 시작하는 시점에 리스크화됨.

**Action**: [senior-fullstack-engineer] `app/privacy/page.tsx` push subscription 항목 추가 — due-2026-05-17로 당김 (CEO VAPID 설정 전 완료) (출처: 2026-05-13 standup §2.2)

### 2.3 senior-qa-engineer

**검증 결과** (`964d50d`):

| 게이트 | 결과 |
|---|---|
| 타입체크 (`npm run lint`) | PASS — clean |
| vitest (`157/157`) | PASS |
| eval-distortion-fix | N/A (AI 분석 흐름 변경 없음) |
| rls-audit | N/A (migration 없음) |
| PII grep | PASS — 로그 집계치만(`sent=N total=N failed=N`), userId 평문 없음 |

**신규 테스트 케이스 검토**: cron.test.ts +3 케이스:
- GET handler export 존재 확인 ✓
- GET + Bearer 200 ✓
- GET + Bearer 누락 → 401 ✓

**미검증 항목**: Vercel 실제 크론 트리거 E2E — 프로덕션 환경 의존 (CEO VAPID 설정 + Vercel 크론 활성화 후). 이 검증은 기존 carry-over `iOS 실기기 push 수신 E2E` 범위와 중첩.

**Action**: [senior-qa-engineer] Vercel 크론 GET 트리거 E2E 검증 — CEO VAPID 설정·Vercel 크론 활성화 완료 후 실 cron 트리거 → push 수신 확인 (출처: 2026-05-13 standup §2.3)

---

## 4. Action items

(Phase 3 추출 — `_actions.md` Open 섹션에 동시 추가됨)

- [ ] [senior-fullstack-engineer] [due-2026-05-17] `app/privacy/page.tsx` push subscription 항목 추가 — CEO VAPID 설정 전 완료, risk-manager P1 격상 권고 (출처: 2026-05-13 standup §2.2)
- [ ] [senior-qa-engineer] [due-2026-05-24] Vercel 크론 GET 트리거 E2E 검증 — CEO VAPID·크론 활성화 완료 후 실 cron 트리거 → push 수신 확인 (출처: 2026-05-13 standup §2.3)

---

## 5. carry-over close

(이번 standup에서 close 판정 없음 — `964d50d`는 CEO carry-over VAPID 설정과 별개 영역)

---

## 0.1 본질 위협 surface 요약

(이번 회의 ⚠️ 0건)
